Add pretty printer to all data types defined in the mli file (for example action).
API should dog food itself, so Config.make should return 't', same for of_strings. 
And both should have language (with polymorphic variants) that describes errors.
Functions that change error (map_kind ) should receive loc, and conversion
point should be recorded.
Write deviations/changes from the .ai/error.md to the error-changes.md with motivation.
Update, if necessary, .ai/devcontainer-cli.md  based on your experience.


Update tests to also include coverage for bytecode ocaml runtime.
Opam says that ocaml 4.14+ is required, find earliest supported ocaml version
that is compatible with the package, and update test matrix in gh.
